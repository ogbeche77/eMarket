import React from 'react';
import CartItem from '/CartItem';

export default function CartList() {
    return (
        <div>
            hello cart list
            <CartItem/>
        </div>
    )
}
